# 运行 go-task 命令 而不是 task 命令
# go-task build

version: "3"

tasks:
  default:
    cmds:
      - clear
      - task: build

  build:
    desc: "Build all components using build script"
    cmds:
      - go run ./cmd/sshole
